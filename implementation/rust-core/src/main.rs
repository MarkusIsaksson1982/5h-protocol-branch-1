fn main() {
    println!("5H Protocol v0.2 – Grok-led Reference Implementation");
    println!("Run the full demo with: cargo run --example full_flow");
    println!("Or start the full stack with Docker Compose from the repo root.");
}
